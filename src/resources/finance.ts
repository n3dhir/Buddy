// v1 stub — resources land after the tool layer is verified.
// Planned:
//   rafiq://expenses/recent          — last 20 expenses as JSON
//   rafiq://budget/current-month     — budget vs actual (needs v2 set_budget)
//   rafiq://expenses/{year}/{month}  — template for a given month
export const FINANCE_RESOURCES_STUB = true;
