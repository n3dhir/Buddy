DATABASE_URL="postgresql://user:password@localhost:5432/rafiq"
PORT=3000
# Required on a public server. Bearer token for /api/* and /mcp.
# Generate: openssl rand -hex 32. Leave unset only for local dev.
RAFIQ_API_TOKEN=""
