DATABASE_URL="postgresql://user:password@localhost:5432/rafiq"
PORT=3000
# Auth for /api/* and /mcp (except /api/health): password login -> JWT.
# Generate the secret: openssl rand -hex 32. Leave both unset only for local dev.
RAFIQ_PASSWORD="change-me"
RAFIQ_JWT_SECRET=""
