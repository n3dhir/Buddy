DATABASE_URL="postgresql://user:password@localhost:5432/rafiq"
PORT=3000
# Auth: users table + password login -> JWT (1y expiry).
# First registrant becomes admin. Close registration after creating yours.
ALLOW_REGISTER="true"
# Signing secret. Generate: openssl rand -hex 32.
RAFIQ_JWT_SECRET=""
